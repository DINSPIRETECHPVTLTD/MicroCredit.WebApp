import { themeQuartz } from 'ag-grid-community';

/** Shared AG Grid theme for all grids in the application. */
export const agGridTheme = themeQuartz.withParams({
  browserColorScheme: 'light',
  headerFontSize: 14,
});
